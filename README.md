# GitLab Tool

Cross-platform GitLab automation CLI. Install with `pip install -e .` then run `gitlab-tool --help`.

Copy `config.example.yaml` to `config.yaml` and `projects.example.yaml` to `projects.yaml`.
